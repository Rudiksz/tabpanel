name: tab_panel_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.9.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  tabpanel:
    path: ../../tabpanel/

dev_dependencies:
  pedantic: ^1.0.0

flutter:
  uses-material-design: true