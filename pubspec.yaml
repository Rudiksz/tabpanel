name: tabpanel
description: Tabbed inferface with support for nested and resizeable panels.
version: 0.1.0
author: Rudolf Martincsek
homepage: https://github.com/Rudiksz/tabpanel

environment:
  sdk: '>=2.9.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  uuid: ^2.2.2
  mobx: 
  flutter_mobx:

dev_dependencies:
  build_runner:
  mobx_codegen: ^1.0.0
  pedantic: ^1.0.0